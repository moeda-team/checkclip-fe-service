# Base URL of the checkclip backend service.
# Used by the axios client in src/services/api.ts.
# Must be prefixed with NEXT_PUBLIC_ because it is read in the browser.
NEXT_PUBLIC_API_BASE_URL=https://checkclip-be-service.onrender.com
